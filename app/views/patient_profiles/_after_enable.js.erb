$("ul.errors").html("");
<% if @user.errors.any? %>
  <% @user.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"));
  <% end %>
<% else %>
  $(".users").html("<%= escape_javascript(render 'index') %>");
  $("#user-enable-modal").modal("hide");
 
<% end %>