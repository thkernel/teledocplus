$("ul.errors").html("");
<% if @organization_type.errors.any? %>
  <% @organization_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".organization-types").html("<%= escape_javascript(render 'index') %>");
  $("#organization-type-modal").modal("hide");
 
<% end %>