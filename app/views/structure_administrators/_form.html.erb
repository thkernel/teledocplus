<%= form_with(model: structure_administrator, local: true) do |form| %>
  <% if structure_administrator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(structure_administrator.errors.count, "error") %> prohibited this structure_administrator from being saved:</h2>

      <ul>
      <% structure_administrator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :structure_id %>
    <%= form.text_field :structure_id %>
  </div>

  <div class="field">
    <%= form.label :profile_id %>
    <%= form.text_field :profile_id %>
  </div>

  <div class="field">
    <%= form.label :administrator %>
    <%= form.check_box :administrator %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
